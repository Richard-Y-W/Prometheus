import{describe,expect,it}from'vitest';import{disabledActionExplanation,findingsBySeverity,shouldHighlight}from'./logic';import type{Finding}from'./types';
const finding={id:'1',severity:'critical',failure_mechanism:'torque',affected_entities:['motor','arm-joint'],data:{}} satisfies Finding;
describe('finding presentation',()=>{it('groups severity without altering results',()=>{expect(findingsBySeverity([finding]).critical).toEqual([finding])});it('highlights affected geometry',()=>{expect(shouldHighlight('motor',[finding])).toBe(true);expect(shouldHighlight('base',[finding])).toBe(false)})})
describe('archived action state',()=>{it('explains why research is disabled',()=>{expect(disabledActionExplanation('research')).toMatch(/versioned Qt review flow/i)});it('explains why execution is disabled',()=>{expect(disabledActionExplanation('run')).toMatch(/Program 01B/i)})})
