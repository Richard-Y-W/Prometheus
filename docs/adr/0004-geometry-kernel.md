# ADR-0004: Open Cascade geometry kernel

Status: accepted. Open Cascade STEP/XDE will own hierarchy-preserving import, B-Rep, measurements, tessellation, and geometric operations. Original B-Rep, viewport mesh, and collision proxy are distinct versioned artifacts. The adapter is feature-gated until OCCT is installed.
