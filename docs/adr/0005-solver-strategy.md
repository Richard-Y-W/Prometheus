# ADR-0005: fidelity ladder and adapters

Status: accepted. Native C++ implements deterministic low-cost checks. MuJoCo is an optional targeted dynamics adapter after cheaper gates. Modelica and cloud FEA/CFD remain contracts/feature flags and cannot block the offline slice or run without explicit authorization.
