# ADR-0001: C++ desktop and Python research service

Status: accepted.

The product baseline used React for UI and Python for engineering calculations. The master architecture requires C++20/Qt 6 to own desktop UI, CAD state, project persistence, native checkers, planning, and adapters; Python owns research. We will migrate incrementally and preserve the verified baseline as a behavioral reference until native parity exists.
